# Testing Capabilities — rifa

## Test Runner
- **Framework**: Vitest 4.1.8
- **Status**: Installed and working (15 tests across 4 test files)
- **Command**: `pnpm test` (in app/ directory)

## Test Layers

| Layer | Tool | Coverage | Status |
|-------|------|----------|--------|
| Unit tests | Vitest | Functions, utilities | ✅ Working |
| Integration tests | Vitest | API routes, KV operations | ✅ Working |
| Component tests | — | — | ❌ Not installed |
| E2E tests | — | — | ❌ Not installed (v2) |

## Test Files

```
src/__tests__/
├── config.test.ts     — GET /api/config
├── confirm.test.ts    — PUT /api/numbers/[num] (confirm, undo, cancel)
├── export.test.ts     — GET /api/export
└── numbers.test.ts    — GET/POST /api/numbers
```

## Coverage Thresholds

| Metric | Target | Rationale |
|--------|--------|-----------|
| Statements | 80% | Core logic must be tested |
| Branches | 75% | State transitions are critical |
| Functions | 85% | All API handlers tested |
| Lines | 80% | Maintain quality baseline |

## Notes

- Strict TDD mode is enabled in openspec config
- Tests mock Redis (no real DB calls in tests)
- No coverage tool installed yet (would need `@vitest/coverage-v8`)
